function Lorena() {
    const Martin = 2 / 3;
    const Jairo = 1 / 4;

    const sumaPartes = Martin + Jairo;

    const parteLorena = 1 - sumaPartes;

    alert(`La parte que le corresponde a Lorena es: ${parteLorena}`);
}

Lorena();